# Приёмка E1: собственный rat-engine — 2026-09-08

Ветка `feat/rat-expedition-engine-architecture`. Пользователь выбрал собственный
движок; продолжаем существующий rat-engine. [Карточка E1](../../vault/production/tasks/expedition-engine-decision.md)
и Sprint 17 закрыты с Approved. Игровой runtime относится к следующему P1.

## Результат

- [ADR-017](../../vault/production/decisions/ADR-017%20Rat%20expedition%20uses%20rat-engine.md):
  C++20, версия проекта 0.1.0, исходный проверенный baseline
  `ecb6fbf84dc06b0ebd87bc40ed86caea192eeee2`; без заявления о стабильном релизе.
- [Архитектура](../rat-expedition-architecture.md): существующие системы и пробелы,
  границы game/core/editor, состояние, authoring, сохранения и доставка.
- [Спецификация P1](../rat-expedition-traversal-spec.md): отдельное игровое приложение,
  дисковый PNG, физическая стойка, лестница, две сцены, безопасные переходы,
  ортографические перекрытия и самостоятельный Windows-пакет.
- Обновлены рабочие указатели GDD и процесса; историческая рекомендация Godot
  сохранена. Десять следующих карточек остаются Not started, без спринта.

## Независимое ревью

Первое ревью `a34023a`: Needs fixes, одна находка — перспективный по смыслу луч
camera→leader не соответствует фиксированной ортографической проекции вне центра.
Исправление `e44c864` задаёт параллельный направлению камеры луч через экранную
позицию героя, ограниченный его глубиной. Добавлена проверка вне центра с
перекрывающей, боковой и находящейся за героем стенами.

Повторное ревью пакета `a34023a` + `e44c864`: **Approved**, новых замечаний нет.
Проверены реальные исходники симуляции, коллизий, replay, renderer, camera,
доступность bimg_decode и CMake-границы. Это проверка документации, не P1 runtime.

## Проверки после закрытия

Полная проверка выполнена ведущим агентом после коммита закрытия `772a63b`.

| Команда / проверка | Фактический результат |
| --- | --- |
| `powershell -ExecutionPolicy Bypass -File scripts/verify.ps1` | Exit 0: configure, полный Release build, CTest, vault и Python |
| CTest dev-release | 714/714 passed, 0 failures, 7,01 с |
| `python -m unittest discover -s scripts/tests` | 16/16 passed |
| `python scripts/check_vault.py` | Passed |
| `python scripts/bmad_vault.py sync`, `check`, `smoke` | Passed после обновления служебной проекции; 41 skill, native resolvers и пустой текущий спринт |
| Локальные Markdown-ссылки в 14 файлах реализации | 43 проверены, все цели существуют |

Журнал: `build/rat-expedition-engine-verify.log`.
Редактор: `C:/5_gamedev/rat-engine/build/dev-release/apps/editor/rat-editor.exe`.
Проверен полный Release target graph; runtime-исходники не менялись, поэтому
актуальные объекты/исполняемый файл не требовали перекомпиляции, выполнено
копирование executable-adjacent data. Это не новая игровая сборка rat-game.
PowerShell оформил stderr прогресса unittest как NativeCommandError в журнале;
unittest сообщил OK, общий процесс завершился с кодом 0.

## Сохранность и ограничения

Все 47 файлов исходного пользовательского снимка сохранены побайтно и исключены
из коммитов этапа. Старые карты/save, runtime и зависимости не изменялись.
После закрытия Sprint 17: current false, доска __NO_ACTIVE_SPRINT__, производная
проекция BMad пуста. Статусы и финальная служебная проекция проверены при записи
этого отчёта; источником очереди остаются карточки vault.

GUI редактора, игра, визуальная приёмка, производительность и remote CI не
проверялись. Конфигурация ПК — инвентаризация, не минимальные требования.
Следующая реализация — [P1](../../vault/production/tasks/expedition-prototype-traversal.md).

## Bugs found

Одна неточность спецификации исправлена и независимо одобрена. Незакрытых находок
нет. Runtime-дефекты на этом документационном этапе не исследовались.
