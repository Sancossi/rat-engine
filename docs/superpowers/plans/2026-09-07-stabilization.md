# Sprint 15 implementation contract

Approved by user 2026-09-07. Implement all audit A01–A16 and four Sprint 14 polish cards plus graph self-pin bug. Audit: ../audits/2026-09-07-project-review.md. No new ECS, renderer rewrite, art importer, field physics, cross-map loader or EventTouch implementation.

## Execution

One implementer at a time, independent read-only reviewer after each coherent commit. Parent owns statuses and closes after review. Continue approved queue without asking. Separate feature branch feat/sprint-15-stabilization. Preserve user edits: build/stabilization/user-before.patch; baseline content modification is extra MGS3 ladder link in feat-ladder-toward-climb-jump-grab.md, and untracked .cursor/rules/rebuild-after-stage-close.mdc. Do not commit those changes. Most other dirty entries are line-ending noise.

## Stages and contracts

1. Foundation A11–A14: root AGENTS.md with shared workflow; Cursor stubs link it. Project .agents/skills rat-build-verify, rat-vault, rat-runtime-repro following skill-creator. Fix unambiguous H1 wikilinks to file|label, resolve missing links without placeholder files. Checker handles fenced code, enums, required fields, zero or one active sprint, current board including bugs. Sprint 15 current, predecessors remain closed. Update actual README, Systems Index and roadmap, not audit history. Windows build wrapper vswhere + CMake presets, no hardcoded installed VS path. Bootstrap testable GUI input/observer architecture only as useful, full implementation stage 6.
2. Storage A02–A04: FileStore::write_atomic separate from ordinary log writes. Unique same-dir temp, completed checked writes, single .bak of previous target, fail save if backup fails. Atomic platform replacement preserves old main until success. No claim of hardware power-loss durability. Memory/failing adapters test write/backup/replace errors. Valid RATSAVE1 dual-read, new JSON schema_version 2 required map_id, position, switches, variables, self_switches, inventory. Unicode/spaces roundtrip. Strict fields, IDs, finite and range validation; transactional load. Semantics progress+position, not resume mid-VM/jump. Public apply rejects empty/mismatched map or nonfinite position. Maps versions 1–5 retained, validate finite geometry/grid arithmetic/IDs across parse/compile/save; incomplete graphs allowed during editing, not apply/save. ADR for changed formats/APIs.
3. Authoring A01/A05/A09 and empty voxel removal: canonical author content clean snapshot, compare after mutations not every frame, preview/compile not dirty; command result ok/changed distinguishes no-op without undo or redo loss. All three create-event paths common unique allocator; duplicate rename fails transactionally. Controller Close/Reload/Load with Save/Discard/Cancel; OS close routed, pause sim and edits while modal; failed save retains modal; input and accumulator reset on dismissal. Dirty in title. Explicit validated .bak restore for map and save slot, no automatic fallback. Restore map obeys unsaved guard. Tests via headless controller and later actual GUI.
4. Runtime A06–A08/A10: reject cross-map transfer and EventTouch with precise MapIssue paths in legacy commands and graph nodes, disabled UI choice and runtime defenses, unsupported authored maps remain openable for repair. Same-map transfer unchanged. Replay schema 2 only, incompatible v1 clear error, header canonical runtime map fingerprint ignoring node layout, runtime/checksum version, seed, full SimulationConfig and start PlayerBody. Fresh-session recording only, validate session preconditions. Strict header/ticks/finite dt and values, sequential nonzero tick IDs, checksum zero is not skip sentinel, recorded dt used. Structured parse/play result errors and first divergence. Hash complete authoritative game/player/jump/input-buffer/VM/node/route/overlays/sets in canonical order, no pointers/render/timings. Fingerprint underlying semantic map; all state needed for subsequent evolution included.
5. Geometry and graph: high ramp face towards neighboring solid in all yaw side-place modes, nondegenerate wedge faces, fences omitted only intended high face. Strengthen schema-2 roundtrip and out-of-grid /occupancy/0 tests; bridge slab support and underpass test fails with filled solid. Wire release always clears dragging including same source/rejected connection.
6. Acceptance A15–A16: fully automated GUI executable reuses real EditorApp/panels/input handlers, scripted frame input shared viewport/ImGui, read-only test selectors/rects/observations, fixed clock/layout/test-only temp project. Never invoke document commands as substitute for user gestures. Test close/reload all modal branches + failure, Play/Edit, text input, ID after restart, undo/noop, voxels/ramps all yaw, bridge over/under, graph connect/delete/self-pin, resize/UI scale 100/150/200. bgfx frame captures, logs + debug snapshots + scenario report on failure. Windows WARP supported by pinned bgfx; Linux Xvfb software GL; no silent skip on init failure. Headless tests remain independently buildable.

Build: add renderer option separately; renderer/editor/gui off means no graphics/audio FetchContent. EditorLaunchOptions --data-root --user-data-dir --map, resolve relative args once at launch. Default data executable-adjacent copied by build/install; user data platform app directory for saves/logs/debug/imgui. Package run from unrelated cwd with source tree inaccessible; fixture writes only temp roots. CMake install/package. CI Windows/Linux Release, Linux Debug ASan/UBSan, headless configure, GUI jobs, vault check, clang-tidy own core/editor-logic (explicit suppressions only), package. Test timeout 120s, GUI 300s, jobs 30m. Full Release build after each stage. Benchmark grey_yard and synthetic maps tick p50/p95/p99, bake/apply, undo memory with machine metadata; report baseline without arbitrary gate or unrelated optimization.

Screenshot skill installed via skill-installer; gh-fix-ci deferred until gh access exists. Mark content import and architectural separation partial/planned with future acceptance, not implemented. Existing agreed art checkbox decisions reconciled, no invented gameplay decisions.

## Completion

Every audit ID and existing included followup mapped to tests/verification and independent approved review. Keep Resolution/Bugs found/evidence on cards. Existing 643 baseline tests plus actual regressions, negative contracts, GUI and package must pass. Remote CI only claim verified if run observed; local equivalent commands and any unavailable external evidence stated precisely.
